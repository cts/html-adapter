module.exports = Parser = {

  parseSelectionSpec: function(selectorString) {
    // TODO: Currently unused, but this is the proper implementation since
    // we rely on the browser's internal libraries to parse CSS selectors.
    return selectorString;
  },

  parseAttributes: function() {

  },

  

};